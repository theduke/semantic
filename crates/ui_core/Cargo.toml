[package]
name = "semantic_ui_core"
authors.workspace = true
keywords.workspace = true
repository.workspace = true
homepage.workspace = true
license.workspace = true
readme.workspace = true
edition.workspace = true
rust-version.workspace = true
categories.workspace = true
version.workspace = true

[features]
default = ["markdown"]
markdown = ["dep:dxeditor", "dep:pulldown-cmark"]

[dependencies]
semantic_rpc_core = { path = "../rpc_core" }
bytes = { workspace = true }
dioxus = { workspace = true, features = ["router"] }
dioxus-icons = "0.1.0"
dioxus-sdk-time = "0.7.0"
dxcomp = { path = "../dxcomp" }
dxform = { path = "../dxform" }
facet-json = "0.43.2"
futures = "0.3.31"
pulldown-cmark = { version = "0.13.0", optional = true }
regex = "1.12.2"
semantic_base = { path = "../base" }
semantic_data = { workspace = true }
semantic_db_core = { path = "../db_core" }
semantic_rpc = { path = "../rpc", features = ["client"] }
thiserror = "2.0.17"
time = { workspace = true, features = ["formatting", "parsing"] }
tracing = { workspace = true }
web-sys = { version = "0.3.82", features = ["Window", "Location"] }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
dxeditor = { path = "../dxeditor", default-features = false, features = ["markdown"], optional = true }

[target.'cfg(target_arch = "wasm32")'.dependencies]
dxeditor = { path = "../dxeditor", default-features = false, features = ["markdown", "web"], optional = true }

[dev-dependencies]
dioxus-html = { version = "0.7.9", features = ["serialize"] }
serde_json = { workspace = true }
semantic_db_kv = { path = "../db_kv" }
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
