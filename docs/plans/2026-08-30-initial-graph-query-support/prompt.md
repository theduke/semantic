this projects database currently supports SQL queries, but supports typed relationships like a graph db.

Investigate:

* which graph query language would be best to support
* how to extend the query execution engine with efficient graph query support, without changing the storage data model

write a full plan to ./docs/plans/<date>-initial-graph-query-support/plan.md

let a subagent do it
